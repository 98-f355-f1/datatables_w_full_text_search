<!DOCTYPE html>
<html>
  <head>
    <title>HotwireDatatables</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://cdn-tailwindcss.vercel.app/?plugins=forms,typography,aspect-ratio,line-clamp"></script>
    <%# <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css"> %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <main class="myprose">
      <header class="header flex justify-center">
        <div class="flash-notices" id="notices">
          <div class="flash-notice" id="notice">
            <%= render FlashComponent.new(flash_notice: @flash_notice) %>
          </div>
        </div>
      </header>
      <%= yield %>
      <%# <div data-controller="geo-location"></div> %>
      <%# SEE: Custom Turbo Confirm MOdals with Hotwire in Rails by GoRails %>
      <dialog id="turbo-confirm" class="bg-slate-100 rounded-lg shadow-md">
        <form method="dialog">
          <p></p>
          <div class="flex justify-end mt-3">
            <button class="bg-transparent hover:bg-red-500 text-red-700 font-semibold hover:text-white mr-1 py-2 px-4 border border-red-500 hover:border-transparent rounded" value="cancel">Cancel</button>
            <button class="bg-transparent hover:bg-blue-500 text-blue-700 font-semibold hover:text-white ml-1 py-2 px-4 border border-blue-500 hover:border-transparent rounded" value="confirm">Confirm</button>
          </div>
        </form>
      </dialog>
    </main>
  </body>
</html>
